# Files to ignore (avoid Mac OS's .DS_store's)
.DS_Store 
# We avoid .pyc files since they are unnecessary
*.pyc

# We don't wan't the database either
BotDatabase.db

# Keeping them .gitignore files incase we ignore enitre content of folder
!.gitignore
